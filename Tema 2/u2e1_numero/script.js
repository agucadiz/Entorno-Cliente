// variable de tipo entero.
const entero=1357;
// variable de tipo decimal.
const decimal=135.7;
// variable de tipo científico.
const cientifico=135e7;
// variable de tipo oscal.
const octal=01357;
// variable de tipo hexadecimal.
const hexadecimal=0x1357;


alert("Entero " + entero);
alert("Decimal " + entero);
alert("Científico " + entero);
alert("Octal " + entero);
alert("Hexadecimal " + entero);
